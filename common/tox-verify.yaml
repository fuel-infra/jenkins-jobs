- job-template:
    name: 'tox-verify-{name}'
    node: '{node}'
    concurrent: true

    builders:
      - shell: 'tox -v'

    scm:
      - main:
          name: '{reponame}'

    triggers:
      - main:
          project-name: '{reponame}'
