- view:
      type: list
      name: 'MCP'
      regex: 'mcp*'
      jobs:
        - prepare_mcp_env
      columns:
        - status
        - weather
        - job
        - last_success
        - last_failure
        - last_duration
        - build_button
      recurse: False

