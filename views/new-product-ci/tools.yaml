- view:
      type: list
      name: 'tools'
      regex: '.*(docker-.*|cleaner.*).*'
      jobs:
        - create-mos-compliance-reports
        - fuel_ci-status_reports
        - iso_deployer
        - prepare_env
        - run_on_node
        - trigger-external-events
        - verify-slave-on-label
      columns:
        - status
        - weather
        - job
        - last_success
        - last_failure
        - last_duration
        - build_button
      recurse: False
