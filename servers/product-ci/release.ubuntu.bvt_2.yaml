- job-template:
    name: '{version-id}.ubuntu.bvt_2'
    builders:
      - inject:
          properties-content: '{custom-parameters}'
      - systest
    concurrent: true
    description: '{description}'
    logrotate:
      daysToKeep: 30
      numToKeep: 50
    node: bvt
    properties:
      - heavy-job:
          weight: 8
      - throttle:
          max-per-node: 1
          option: project
    parameters:
      - systest:
          dist: Ubuntu
          testgroup: bvt_2
          version-id: '{version-id}'
          id: '{id}'
    publishers:
      - nosetests
      - email-systest:
          body-failed:
            !include-raw notifications/bvt.failed
          mail-to: devops+alert@mirantis.com,fuel-core-team@mirantis.com
    scm:
      - stackforge:
          branch: '{branch}'
          repo: fuel-main
    wrappers:
      - timeout:
          timeout: 200