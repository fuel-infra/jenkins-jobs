- project:
    name: '9.0 custom iso'
    version: '9.0'
    branch: 'origin/stable/mitaka'
    default_commit: 'stable/mitaka'
    defaults: '{version}.custom.iso'
    description:
      !include-raw descriptions/custom.iso
    jobs:
      - '{version}.custom.iso'
