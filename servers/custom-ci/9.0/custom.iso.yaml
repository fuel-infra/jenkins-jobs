- project:
    name: '9.0 custom iso'
    version: '9.0'
    default_commit: 'master'
    defaults: '{version}.custom.iso'
    description:
      !include-raw descriptions/custom.iso
    jobs:
      - '{version}.custom.iso'
