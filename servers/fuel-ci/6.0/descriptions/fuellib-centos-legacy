Runs <a href="http://docs.mirantis.com/fuel-dev/devops.html#environment-creation-via-devops-fuel-main">system_test.sh</a> with '--group=deploy_neutron_gre' and parameters:<p>
<pre>
  OPENSTACK_RELEASE=CentOS
  DEPLOYMENT_MODE="multinode"
  UPLOAD_MANIFESTS=true
  UPLOAD_MANIFESTS_PATH=/path/to/fuel-library/deployment/puppet/
</pre>
<p>
<pre>
Scenario:
1. Create cluster
2. Add 1 node with controller role
3. Add 2 nodes with compute role
4. Deploy the cluster
5. Run network verification
6. Run OSTF
</pre>
