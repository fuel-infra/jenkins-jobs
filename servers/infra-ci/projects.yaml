- project:
    name: 'jenkins-jobs'
    reponame: 'fuel-infra/jenkins-jobs'
    jobs:
      - 'tox-verify-{name}':
              node: 'tox'

- project:
    name: 'centos'
    jobs:
      - 'mirror-local-{name}':
              node: 'mirror'

